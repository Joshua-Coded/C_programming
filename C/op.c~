#include <stdio.h>

int number;
int result;

int main()

{

  number = 5;
  result = ++number;
  printf("the RESULT IS: %d\n", number);
  result = number++;
  printf("THE RESULT IS: %d\n", number);
  return (0);


  }
